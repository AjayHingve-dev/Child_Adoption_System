import React,{useState} from 'react';
import {NavLink,useNavigate} from 'react-router-dom';
import {LayoutDashboard,Users,HeartHandshake,Baby,ClipboardList,House,BarChart3,Settings,ShieldCheck,LogOut,Menu,X,UserCog,UserRound} from 'lucide-react';
import {api} from '../api';
import {clearSession,getUser} from '../auth';
const baseLinks=[['/dashboard',LayoutDashboard,'Dashboard'],['/profile',UserRound,'My Profile'],['/parents',Users,'Parents'],['/children',Baby,'Children'],['/applications',ClipboardList,'Applications'],['/home-visits',House,'Home Visits'],['/social-workers',HeartHandshake,'Social Workers'],['/reports',BarChart3,'Reports'],['/settings',Settings,'Settings']];
export default function Layout({children}){const [open,setOpen]=useState(false);const nav=useNavigate();const user=getUser();const links=user?.role==='SUPER_ADMIN'?[...baseLinks.slice(0,7),['/admins',UserCog,'Admins'],...baseLinks.slice(7)]:baseLinks;const logout=async()=>{try{await api.post('/auth/logout')}catch{}finally{clearSession();nav('/login',{replace:true})}};return <div className="app-shell"><aside className={`sidebar ${open?'open':''}`}><div className="brand"><div className="brand-mark"><HeartHandshake/></div><div><b>Aashray</b><small>Adoption Admin</small></div><button className="mobile-close" onClick={()=>setOpen(false)}><X/></button></div><nav>{links.map(([to,Icon,label])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>isActive?'active':''}><Icon size={19}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-foot"><NavLink to="/profile" className="mini-profile profile-link"><div className="avatar">{user?.fullName?.[0]||'A'}</div><div><strong>{user?.fullName||'Administrator'}</strong><small>{user?.role||'ADMIN'}</small></div></NavLink><button className="logout" onClick={logout}><LogOut size={18}/> Sign out</button></div></aside><main className="main"><header className="topbar"><button className="menu-btn" onClick={()=>setOpen(true)}><Menu/></button><div className="top-title"><ShieldCheck size={19}/><span>Secure Administration Portal</span></div><NavLink to="/profile" className="top-user"><span>{user?.email}</span><div className="avatar small">{user?.fullName?.[0]||'A'}</div></NavLink></header><div className="content">{children}</div></main>{open&&<div className="scrim" onClick={()=>setOpen(false)}/>}</div>}
