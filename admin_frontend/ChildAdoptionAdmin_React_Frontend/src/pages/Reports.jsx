import React,{useState} from 'react';import {FileBarChart,Home,GitCompare,BadgeCheck,CalendarDays,Activity,Download} from 'lucide-react';import {api,errorMessage} from '../api';import {PageHeader,Card,Button,Loading,Toast} from '../components/UI';
const reports=[['application-summary',FileBarChart,'Application summary','Status-wise application performance'],['home-visits',Home,'Home visit report','Scheduled and completed assessments'],['child-matching',GitCompare,'Child matching','Matching and availability overview'],['adoption-decisions',BadgeCheck,'Adoption decisions','Approval and rejection outcomes'],['monthly-activity',CalendarDays,'Monthly activity','Yearly month-by-month movement'],['user-activity',Activity,'User activity','Registration and engagement details']];
export default function Reports(){const [data,setData]=useState(null),[active,setActive]=useState(''),[loading,setLoading]=useState(false),[toast,setToast]=useState(null);const run=async key=>{setLoading(true);setActive(key);try{const params=key==='monthly-activity'?{year:new Date().getFullYear()}:{};const r=await api.get(`/reports/${key}`,{params});setData(r.data)}catch(e){setToast({type:'error',message:errorMessage(e)})}finally{setLoading(false)}};const download=()=>{const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`${active}-report.json`;a.click();URL.revokeObjectURL(url)};return <><PageHeader title="Reports & insights" description="Generate operational reports to support informed adoption decisions."/><div className="report-grid">{reports.map(([key,Icon,title,text])=><Card key={key} className={`report-card ${active===key?'selected':''}`}><div className="report-icon"><Icon/></div><h3>{title}</h3><p>{text}</p><Button variant="secondary" onClick={()=>run(key)}>Generate report</Button></Card>)}</div>{active&&<Card className="report-result"><div className="card-title"><div><span className="eyebrow">Generated report</span><h2>{reports.find(r=>r[0]===active)?.[2]}</h2></div>{data&&<Button variant="secondary" onClick={download}><Download/> Download JSON</Button>}</div>{loading?<Loading/>:<pre>{JSON.stringify(data,null,2)}</pre>}</Card>}<Toast toast={toast} onClose={()=>setToast(null)}/></>}
